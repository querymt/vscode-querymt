name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Package VSIX
        run: npm run package

  protocol-tests:
    runs-on: ubuntu-latest
    if: vars.CODER_AGENT_BIN != ''
    needs: lint-and-build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run protocol integration tests
        run: npm run test:protocol
        env:
          CODER_AGENT_BIN: ${{ vars.CODER_AGENT_BIN }}
          CODER_AGENT_CONFIG: ${{ vars.CODER_AGENT_CONFIG }}

  extension-host-tests:
    runs-on: ubuntu-latest
    if: vars.CODER_AGENT_BIN != ''
    needs: lint-and-build
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run extension host smoke tests
        run: xvfb-run -a npm run test:extension
        env:
          CODER_AGENT_BIN: ${{ vars.CODER_AGENT_BIN }}
          CODER_AGENT_CONFIG: ${{ vars.CODER_AGENT_CONFIG }}
